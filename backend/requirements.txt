# Core FastAPI Stack
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0
python-multipart>=0.0.20

# Database
motor>=3.7.0
pymongo>=4.10.0

# Caching & Queue
valkey>=6.1.0
redis>=5.0.0
celery>=5.4.0

# Object Storage
minio>=7.2.14

# HTTP Client
httpx>=0.28.0
requests>=2.32.0

# Logging & Monitoring
structlog>=24.4.0
prometheus-client>=0.21.0
prometheus-fastapi-instrumentator>=7.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
hypothesis>=6.100.0

# Utilities
python-dotenv>=1.0.0
email-validator>=2.2.0

# Data Processing (for video/3D pipeline)
numpy>=1.26.0,<2.0.0
pillow>=10.0.0
opencv-python>=4.9.0

# Video Processing
ffmpeg-python>=0.2.0

# AI/ML Models (for depth estimation)
torch>=2.0.0
torchvision>=0.15.0

# 3D Processing
open3d>=0.18.0
trimesh>=4.0.0
plyfile>=1.0.0
scipy>=1.11.0

# Geospatial
pyproj>=3.6.0

# PDF Generation
reportlab>=4.0.0

# GDAL for orthophoto processing (optional, complex installation)
# GDAL>=3.8.0
# Note: GDAL installation on Windows requires pre-built wheels or OSGeo4W
# For production orthophoto support, install GDAL separately:
# pip install GDAL==3.8.0 (or use conda: conda install -c conda-forge gdal)

# Image Processing & Segmentation
scikit-image>=0.22.0

# CLIP for classification (optional, install separately for full support)
# pip install git+https://github.com/openai/CLIP.git

# Segment Anything (optional, install separately)
# pip install git+https://github.com/facebookresearch/segment-anything.git
